import { PACKAGE_NAME } from '../../utils/constants';

export const DATA_VERSION_FIELD = '1.0.0';
export const DATA_TYPE_FIELD = PACKAGE_NAME;

export const REQUEST_INTERVAL_KEY = 'requestInterval';
export const MINIMUM_REQUEST_INTERVAL = 1000;
export const DEFAULT_REQUEST_INTERVAL = 5000;
