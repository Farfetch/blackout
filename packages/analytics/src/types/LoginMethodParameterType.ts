/**
 * Contain login method types to be used when tracking the login (or sign-up)
 * events.
 */
enum LoginMethodParameterType {
  Tenant = 'Tenant',
  Facebook = 'Facebook',
  Google = 'Google',
  Twitter = 'Twitter',
  Apple = 'Apple',
  Other = 'Other',
}

export default LoginMethodParameterType;
