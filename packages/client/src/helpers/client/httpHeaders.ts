/**
 * HTTP request header names to apply to the requests.
 */
export enum HttpHeaders {
  AcceptLanguage = 'Accept-Language',
  ContentType = 'Content-Type',
  FFCountry = 'FF-Country',
  FFCurrency = 'FF-Currency',
}
