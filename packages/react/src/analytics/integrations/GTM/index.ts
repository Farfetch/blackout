export { default } from './GTM.js';

// To allow the user to configure its own event schemas. Export yup as a different name.
export { validationSchemaBuilder } from '../shared/validation/eventSchemas.js';

export * from './types/index.js';
