export type Publisher = {
  // The name of the Article publisher.
  name: string;
  // The url of the Article publisher.
  url: string;
  // The logo path of the Article publisher.
  logo: string;
};
