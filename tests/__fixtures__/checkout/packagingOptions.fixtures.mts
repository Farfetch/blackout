export const mockPackagingOption = 'Basic';

export const mockPackagingOptionsResponse = [mockPackagingOption, 'Signature'];

export const mockPackagingOptionsState = {
  packagingOptions: {
    error: null,
    isLoading: false,
    result: mockPackagingOptionsResponse,
  },
};
